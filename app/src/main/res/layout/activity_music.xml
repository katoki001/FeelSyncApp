<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="4dp"
    android:background="#F7F7F7">

    <!-- MaterialButton to open music file picker with modern style -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_select_music"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Select Music"
        android:textColor="@android:color/white"
        android:background="@drawable/buttn_background"
        android:padding="12dp"
        android:layout_marginTop="16dp"
        android:textSize="16sp"/>

    <!-- TextView to display selected music file name with a better look -->
    <TextView
        android:id="@+id/tv_selected_music"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="No music selected"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#333333"
        android:ellipsize="end"
        android:maxLines="1" />

    <!-- MaterialButton to play/pause music with modern look -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_play_pause"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Play"
        android:textColor="@android:color/white"
        android:background="@drawable/buttn_background"
        android:padding="12dp"
        android:textSize="16sp"/>

    <!-- SeekBar to control playback progress -->
    <SeekBar
        android:id="@+id/seek_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:max="100"
        android:progressDrawable="@drawable/seekbar_progress"
        android:thumb="@drawable/seekbar_thumb" />

    <!-- BottomNavigationView for better navigation design -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="450dp"
        android:layout_alignParentBottom="true"
        android:background="@color/American_blue"
        app:itemIconSize="30dp"
        app:menu="@menu/bottom_nav_menu" />
</LinearLayout>

